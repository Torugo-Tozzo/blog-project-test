<%- include("partials/header") %>
<h1>Bem Vindo</h1>
<p> <%= homeContent %></p>




<% posts.forEach(function(post) { %>
    <h1> <%= post.titulo %> </h1> 
    <p> <%= post.texto.slice(0, 100) + (post.texto.length > 100 ? "..." : "") %> <a href="posts/<%= post.titulo %>">Read more</a></p>
<%}); %>

<%- include("partials/footer") %>